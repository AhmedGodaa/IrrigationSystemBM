apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "irrigation-chart.fullname" . }}-test-connection"
  labels:
    {{- include "irrigation-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "irrigation-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
