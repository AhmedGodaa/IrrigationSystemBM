apiVersion: v1
kind: Service
metadata:
  name: {{ include "irrigation-chart.fullname" . }}
  labels:
    {{- include "irrigation-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "irrigation-chart.selectorLabels" . | nindent 4 }}
